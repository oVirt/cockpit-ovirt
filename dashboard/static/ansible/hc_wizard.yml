---

# Gluster deployment start here
- name: Setup backend
  hosts: hc_nodes
  remote_user: root
  gather_facts: yes
  any_errors_fatal: true
  max_fail_percentage: 0

  roles:
     - gluster.infra
     - gluster.features
